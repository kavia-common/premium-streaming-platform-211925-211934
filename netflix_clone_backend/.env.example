# Port for the backend server
PORT=4000

# Required admin API token used to access any /admin routes (sent as Bearer token)
ADMIN_API_TOKEN=change_me_to_a_long_random_value

# Allowed origin for CORS (frontend dev server)
CORS_ORIGIN=http://localhost:3000
